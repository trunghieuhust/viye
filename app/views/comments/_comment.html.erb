<%= div_for comment do %>
        
                <!-- <strong>
                        Posted <%= time_ago_in_words(comment.created_at) %> ago by <%= comment.user.email %>
                </strong>e
                <br/> -->
                <p class="wrap-ava-1">
                <a class="wrap-img" href="#"><%= image_tag("ava_demo.png", :width=>"100%", :height=>"100%") %></a>
              </p>
              <div class="left-bubble">
                <p><a class="author" href="#"><%= comment.user.name %></a><span class="posted-time"><%= time_ago_in_words(comment.created_at) %></span></p>
                <p class="cmt-content"><%= comment.body %></p>
              </div>
                
                <%if comment.user == current_user || current_user.users_type == 0 || current_user.users_type == 1%>
                <%= link_to 'xóa', comment, method: :delete, data: { confirm: 'Are you sure?' } %>
        		<%end%>
        
<% end %>
